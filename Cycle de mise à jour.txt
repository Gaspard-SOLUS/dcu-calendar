cd ~/Documents/00-Projets/dcu-calendar

# 1. MyTimetable → refresh → Multiple weeks → semaines 1-12 → EXCEL
#    Écraser Timetables.xlsx

# 2. Voir ce qui a bougé
python3 dcu_to_ics.py --diff DCU_Semester1_2026.ics

# 3. Reconstruire si le diff convient
python3 dcu_to_ics.py

# 4. Publier
git add -A
git status
git commit -m "Timetable refresh $(date +%F)"
git push

# Note : depuis l'ajout de .github/workflows/build.yml, un simple
# `git add Timetables.xlsx && git commit ... && git push` suffit — l'Action
# regénère et commit DCU_Semester1_2026.ics toute seule. Les étapes 2-4
# ci-dessus restent utiles pour voir le diff en local avant de pousser.